require "spec_helper"

RSpec.describe Dingtalk::Notifier do
  it "has a version number" do
    expect(Dingtalk::Notifier::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
